import Image from "next/image"; import Link from "next/link"; import type {Article} from "@/lib/data";
export default function NewsCard({article,compact=false}:{article:Article;compact?:boolean}){return <article className={compact?"newsCard compact":"newsCard"}><Link href={`/news/${article.slug}`} className="imageWrap"><Image src={article.image} alt={article.title} width={900} height={520} sizes="(max-width: 768px) 100vw, 33vw"/></Link><div className="cardBody"><span className="categoryLabel">{article.category}</span><h3><Link href={`/news/${article.slug}`}>{article.title}</Link></h3>{!compact&&<p>{article.excerpt}</p>}<time>{article.publishedAt}</time></div></article>}
