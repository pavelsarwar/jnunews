import Image from "next/image"; import {notFound} from "next/navigation"; import Header from "@/components/Header"; import {articles} from "@/lib/data";
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=articles.find(item=>item.slug===slug);if(!article)notFound();return <main><Header/><article className="container articlePage"><span className="categoryLabel">{article.category}</span><h1>{article.title}</h1><p className="articleExcerpt">{article.excerpt}</p><div className="articleMeta">JnU News ডেস্ক · {article.publishedAt}</div><Image className="articleImage" src={article.image} alt={article.title} width={1400} height={760} priority/><div className="articleBody"><p>{article.excerpt}</p><p>JnU News-এর এই ডেমো আর্টিকেল পেজটি পরবর্তী ধাপে database-driven newsroom workflow-এর সঙ্গে যুক্ত হবে। Admin panel থেকে লেখা, ছবি, category, tag, reporter, publish status এবং SEO তথ্য নিয়ন্ত্রণ করা যাবে।</p></div></article></main>}
